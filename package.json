{
  "dependencies": {
    "kaboom": "^3000.1.17"
  },
  "devDependencies": {
    "esbuild": "0.20.0",
    "npm-run-all": "^4.1.5"
  },
  "scripts": {
    "build": "esbuild src/main.js --bundle --outfile=src/build.js",
    "start": "npm run build:watch",
    "build:watch": "esbuild src/main.js --bundle --outfile=src/build.js --watch",
    "serve": "live-server"
  }
}
